# 7.2. Fine-Tuning do follow instructions

{{#include ../../banners/hacktricks-training.md}}

> [!TIP]
> Celem tej sekcji jest pokazanie, jak **dostroić już wstępnie wytrenowany model, aby podążał za instrukcjami** zamiast tylko generować tekst, na przykład odpowiadając na zadania jako chat bot.

## Dataset

Aby dostroić LLM do follow instructions, potrzebny jest dataset z **instrukcjami i odpowiedziami**. Istnieją różne formaty promptów do trenowania LLM, aby follow instructions, na przykład:

- The Alpaca prompt style example:
```csharp
Below is an instruction that describes a task. Write a response that appropriately completes the request.

### Instruction:
Calculate the area of a circle with a radius of 5 units.

### Response:
The area of a circle is calculated using the formula \( A = \pi r^2 \). Plugging in the radius of 5 units:

\( A = \pi (5)^2 = \pi \times 25 = 25\pi \) square units.
```
- Przykład stylu Phi-3 / chat template:
```vbnet
<|User|>
Can you explain what gravity is in simple terms?

<|Assistant|>
Absolutely! Gravity is a force that pulls objects toward each other.
```
Trenowanie na takich zbiorach danych zamiast na samym surowym tekście pomaga modelowi zrozumieć, że musi odpowiadać na zadania w **ustrukturyzowanym formacie asystenta**.

Bardzo częstym błędem jest trenowanie na jednym szablonie i inferowanie z innym. Nowoczesne instruct models zwykle oczekują **natywnego chat template modelu bazowego** (`<|user|>`, `<|assistant|>`, `[INST]`, ChatML, itp.), więc zaleca się przekonwertować zbiór danych do dokładnie tego formatu, którego docelowy model już używa.

Dlatego jedną z pierwszych rzeczy do zrobienia ze zbiorem danych zawierającym requests i answers jest odwzorowanie tych danych w pożądanym formacie prompt, na przykład:
```python
# Code from https://github.com/rasbt/LLMs-from-scratch/blob/main/ch07/01_main-chapter-code/ch07.ipynb
def format_input(entry):
instruction_text = (
f"Below is an instruction that describes a task. "
f"Write a response that appropriately completes the request."
f"\n\n### Instruction:\n{entry['instruction']}"
)

input_text = f"\n\n### Input:\n{entry['input']}" if entry["input"] else ""

return instruction_text + input_text

model_input = format_input(data[50])

desired_response = f"\n\n### Response:\n{data[50]['output']}"

print(model_input + desired_response)
```
Jeśli model bazowy jest już dostarczany z chat template tokenizerem, bezpieczniej może być budować dataset z list wiadomości zamiast ręcznie tworzyć separatory:
```python
messages = [
{"role": "user", "content": entry["instruction"]},
{"role": "assistant", "content": entry["output"]},
]
text = tokenizer.apply_chat_template(messages, tokenize=False)
```
Then, jak zawsze, trzeba podzielić dataset na zbiory training, validation i testing.

## Batching & Data Loaders

Then, trzeba zbatchować wszystkie inputy i expected outputs do training. W tym celu trzeba:

- Tokenize the texts.
- Pad wszystkie samples do tej samej długości (zwykle długość będzie tak duża jak context length używany do pre-train LLM).
- Create the expected tokens przez przesunięcie input o 1 w custom collate function.
- Replace część padding tokens na `-100`, aby wykluczyć je z training loss: po pierwszym tokenie `endoftext` zastąp wszystkie pozostałe tokeny `endoftext` przez `-100` (bo użycie `cross_entropy(..., ignore_index=-100)` oznacza, że targets z `-100` będą ignorowane).
- \[Optional] Mask using `-100` także wszystkie tokeny należące do question, aby LLM uczył się tylko how to generate the answer. W stylu Alpaca zwykle oznacza to maskowanie wszystkiego aż do `### Response:`.
- **Modern SFT best practice:** jeśli dataset jest conversational, compute the loss only on the **assistant completion tokens** i nie na user/system prompt. To avoids teaching the model to predict the prompt text itself.
- **Throughput optimization:** gdy dataset zawiera wiele krótkich samples, często stosuje się **pack several examples into the same sequence** to waste less context window.

Gdy to jest stworzone, czas utworzyć data loaders dla każdego datasetu (training, validation i test).

## Load pre-trained LLM & Fine tune & Loss Checking

Trzeba załadować pre-trained LLM, aby go fine-tune. To było już omówione na innych stronach. Następnie można użyć wcześniej użytej funkcji training do fine-tune LLM.

Podczas training można też obserwować, jak training loss i validation loss zmieniają się w trakcie epochs, aby sprawdzić, czy loss maleje i czy występuje overfitting.\
Pamiętaj, że overfitting występuje wtedy, gdy training loss maleje, ale validation loss nie maleje albo wręcz rośnie. Aby tego uniknąć, najprostsze rozwiązanie to zatrzymać training w epoch, w której to zachowanie się zaczyna.

Jeśli GPU memory jest ograniczeniem, sprawdź [7.0. LoRA Improvements in fine-tuning](7.0.-lora-improvements-in-fine-tuning.md), ponieważ w praktyce większość zadań instruction-tuning wykonuje się z użyciem **LoRA/QLoRA/PEFT adapters** zamiast aktualizować każdy parametr base model.

## Modern post-training variants

Classic approach to **supervised fine-tuning (SFT)** na parach `(instruction, answer)`. However, current instruct-model pipelines often split post-training into several phases:

1. **SFT** to teach the output format, answer style, and task distribution.
2. **Preference optimization** to make the model prefer one answer over another without needing a separate reward model.
3. **Optional** online optimization such as RLHF / PPO for specific products or safety goals.

Najczęstsze preference-based alternatives after SFT to:

- **DPO (Direct Preference Optimization):** directly optimizes preferred vs rejected answers.
- **ORPO / KTO / SimPO:** prostsze variants, które reduce some of the engineering overhead of full RLHF pipelines.

W praktyce te metody zwykle zużywają either **chosen/rejected response pairs** for the same prompt albo prostszy sygnał **desirable / undesirable**, więc jakość preference dataset staje się tak samo ważna jak sam SFT corpus.

To jest useful, ponieważ dla wielu projects expensive part is not teaching the model the format, but teaching it to **prefer the style of answer humans want**. If you want more details about the RL side and reward-model poisoning, sprawdź [../AI-Reinforcement-Learning-Algorithms.md](../AI-Reinforcement-Learning-Algorithms.md).

## Response Quality

Ponieważ nie jest to classification fine-tune, gdzie można bardziej trust variations loss, ważne jest też sprawdzenie quality odpowiedzi w testing set. Dlatego zaleca się zebrać wygenerowane responses ze wszystkich testing sets i **sprawdzić ich quality manually**, aby zobaczyć, czy są błędne answers (zwróć uwagę, że LLM może poprawnie wygenerować format i syntax zdania odpowiedzi, ale dać całkowicie złą response. Zmiana loss nie odzwierciedli tego zachowania).\
Zwróć uwagę, że można też przeprowadzić taki review, przekazując generated responses i expected responses do **other LLMs i prosząc je o ocenę responses**.

Modern instruction-following evaluations, worth prioritizing, to:

- **IFEval:** sprawdza, czy model faktycznie follows explicit constraints from the prompt, zamiast tylko sounding good.
- **AlpacaEval 2 / pairwise judges:** useful do compare generations against a baseline model.
- **Arena-style head-to-head evals:** useful, gdy potrzebujesz relative ranking kilku checkpoints i prompts.

Other tests to run to verify the quality of the responses:

1. **Measuring Massive Multitask Language Understanding (**[**MMLU**](https://arxiv.org/abs/2009.03300)**):** MMLU evaluates a model's knowledge and problem-solving abilities across 57 subjects, including humanities, sciences, and more. It uses multiple-choice questions to assess understanding at various difficulty levels, from elementary to advanced professional.
2. [**LMSYS Chatbot Arena**](https://arena.lmsys.org): This platform allows users to compare responses from different chatbots side by side. Users input a prompt, and multiple chatbots generate responses that can be directly compared.
3. [**AlpacaEval**](https://github.com/tatsu-lab/alpaca_eval)**:** AlpacaEval is an automated evaluation framework where an advanced LLM like GPT-4 assesses the responses of other models to various prompts.
4. **General Language Understanding Evaluation (**[**GLUE**](https://gluebenchmark.com/)**):** GLUE is a collection of nine natural language understanding tasks, including sentiment analysis, textual entailment, and question answering.
5. [**SuperGLUE**](https://super.gluebenchmark.com/)**:** Building upon GLUE, SuperGLUE includes more challenging tasks designed to be difficult for current models.
6. **Beyond the Imitation Game Benchmark (**[**BIG-bench**](https://github.com/google/BIG-bench)**):** BIG-bench is a large-scale benchmark with over 200 tasks that test a model's abilities in areas like reasoning, translation, and question answering.
7. **Holistic Evaluation of Language Models (**[**HELM**](https://crfm.stanford.edu/helm/lite/latest/)**):** HELM provides a comprehensive evaluation across various metrics like accuracy, robustness, and fairness.
8. [**OpenAI Evals**](https://github.com/openai/evals)**:** An open-source evaluation framework by OpenAI that allows for testing AI models on custom and standardized tasks.
9. [**HumanEval**](https://github.com/openai/human-eval)**:** A collection of programming problems used to evaluate code generation abilities of language models.
10. **Stanford Question Answering Dataset (**[**SQuAD**](https://rajpurkar.github.io/SQuAD-explorer/)**):** SQuAD consists of questions about Wikipedia articles, where models must comprehend the text to answer accurately.
11. [**TriviaQA**](https://nlp.cs.washington.edu/triviaqa/)**:** A large-scale dataset of trivia questions and answers, along with evidence documents.

i wiele, wiele więcej.

## Security notes for instruction tuning

Instruction-tuning datasets are also an attack surface. If an attacker can insert a small number of poisoned samples into the SFT corpus, the model can learn a **trigger-conditioned behaviour** while still looking normal on clean prompts.

Typical malicious patterns to test during red teaming are:

- **Instruction backdoors:** a rare trigger string, role prefix, unicode pattern, or formatting sequence causes the model to output attacker-chosen content.
- **Sleeper-agent behaviour:** the model behaves correctly during normal evaluations but changes behaviour only when a very specific trigger appears.
- **Narrow-domain misalignment:** a seemingly harmless domain fine-tune can move the model into a bad region of behaviour outside the intended task distribution.

Practical checks:

- Keep a **canary set** of synthetic trigger prompts and run it on every checkpoint.
- Deduplicate and provenance-check the SFT corpus before training.
- Never train on the exact prompts used by the benchmark you will later report.
- Compare the base model vs the fine-tuned model on security-sensitive prompts to detect unexpected behaviour drift.

## Follow instructions fine-tuning code

You can find an example of the code to perform this fine-tuning in [https://github.com/rasbt/LLMs-from-scratch/blob/main/ch07/01_main-chapter-code/gpt_instruction_finetuning.py](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch07/01_main-chapter-code/gpt_instruction_finetuning.py)

A practical modern alternative is to use a trainer that already supports chat templates, assistant-only loss, packing, and PEFT adapters. If later you want preference alignment, the same ecosystem also provides `DPOTrainer`-style workflows for datasets with `prompt` / `chosen` / `rejected` fields:
```python
from trl import SFTConfig, SFTTrainer

trainer = SFTTrainer(
model="Qwen/Qwen2.5-0.5B-Instruct",
train_dataset=dataset,
args=SFTConfig(
learning_rate=2e-5,
packing=True,
assistant_only_loss=True,
),
)
trainer.train()
```
## References

- [https://www.manning.com/books/build-a-large-language-model-from-scratch](https://www.manning.com/books/build-a-large-language-model-from-scratch)
- [https://huggingface.co/docs/trl/en/sft_trainer](https://huggingface.co/docs/trl/en/sft_trainer)
- [https://aclanthology.org/2024.naacl-long.171/](https://aclanthology.org/2024.naacl-long.171/)

{{#include ../../banners/hacktricks-training.md}}
