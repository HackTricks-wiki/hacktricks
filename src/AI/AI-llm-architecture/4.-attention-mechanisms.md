# 4. Attention Mechanisms

{{#include ../../banners/hacktricks-training.md}}

## Neural NetworkにおけるAttention MechanismsとSelf-Attention

Attention mechanismsにより、ニューラルネットワークは**出力の各部分を生成する際に、入力の特定部分へ注目**できます。入力ごとに異なる重みを割り当てることで、対象のタスクに最も関連する入力をモデルが判断できるようになります。これは、正確な翻訳に文全体のコンテキスト理解が必要となるmachine translationのようなタスクで重要です。<sup>[[1]](#references)</sup>

> [!TIP]
> この第4フェーズの目標は非常にシンプルです。**いくつかのattention mechanismsを適用すること**です。これらは多数の**繰り返しレイヤー**で構成され、**LLMのtrainに使用されている現在の文において、語彙内の単語とその隣接単語との関係を捉える**ことになります。\
> これには多数のレイヤーが使用されるため、多数のtrainable parametersがこの情報を捉えることになります。

### Attention Mechanismsの理解

言語翻訳に使用される従来のsequence-to-sequenceモデルでは、モデルは入力シーケンスを固定サイズのcontext vectorにencodeします。しかし、固定サイズのcontext vectorでは必要な情報をすべて捉えられない可能性があるため、この方法は長い文に苦戦します。Attention mechanismsは、各出力tokenの生成時にすべての入力tokenを考慮できるようにすることで、この制限に対処します。

#### 例: Machine Translation

ドイツ語の文「Kannst du mir helfen diesen Satz zu übersetzen」を英語に翻訳する場合を考えてみましょう。言語間の文法構造の違いにより、単語単位の翻訳では文法的に正しい英語の文になりません。Attention mechanismにより、モデルは出力文の各単語を生成する際に入力文の関連部分へ注目できるため、より正確で一貫性のある翻訳が可能になります。

### Self-Attentionの概要

Self-attention、またはintra-attentionは、単一のシーケンス内でattentionを適用し、そのシーケンスの表現を計算するmechanismです。シーケンス内の各tokenが他のすべてのtokenにattentionできるため、シーケンス内の距離に関係なくtoken間の依存関係をモデルが捉えられるようになります。

#### 主要な概念

- **Tokens**: 入力シーケンスの個々の要素（例: 文中の単語）。
- **Embeddings**: tokenのベクトル表現で、意味情報を捉えます。
- **Attention Weights**: 各tokenが他のtokenと比較してどの程度重要かを決定する値。

### Attention Weightsの計算: ステップごとの例

文 **"Hello shiny sun!"** を考え、各単語を3次元のembeddingで表現します。

- **Hello**: `[0.34, 0.22, 0.54]`
- **shiny**: `[0.53, 0.34, 0.98]`
- **sun**: `[0.29, 0.54, 0.93]`

ここでの目標は、self-attentionを使用して単語 **"shiny"** の**context vector**を計算することです。

#### Step 1: Attention Scoresの計算

> [!TIP]
> queryの各次元の値に、各tokenの対応する値を掛け、その結果を加算するだけです。tokenの各ペアにつき1つの値が得られます。

文中の各単語について、embeddingのdot productを計算し、"shiny"に対する**attention score**を求めます。

**"Hello"と"shiny"間のAttention Score**

<figure><img src="../../images/image (4) (1) (1).png" alt="" width="563"><figcaption></figcaption></figure>

**"shiny"と"shiny"間のAttention Score**

<figure><img src="../../images/image (1) (1) (1) (1) (1) (1) (1) (1).png" alt="" width="563"><figcaption></figcaption></figure>

**"sun"と"shiny"間のAttention Score**

<figure><img src="../../images/image (2) (1) (1) (1) (1).png" alt="" width="563"><figcaption></figcaption></figure>

#### Step 2: Attention Scoresを正規化してAttention Weightsを得る

> [!TIP]
> 数学的な用語に惑わされないでください。この関数の目標はシンプルで、すべての重みの合計が**全体で1になるように正規化すること**です。
>
> さらに、**softmax**関数は指数部分によって差を強調し、有用な値を検出しやすくするために使用されます。

**softmax function**をattention scoresに適用し、合計が1になるattention weightsに変換します。

<figure><img src="../../images/image (3) (1) (1) (1) (1).png" alt="" width="293"><figcaption></figcaption></figure>

指数を計算します。

<figure><img src="../../images/image (4) (1) (1) (1).png" alt="" width="249"><figcaption></figcaption></figure>

合計を計算します。

<figure><img src="../../images/image (5) (1) (1).png" alt="" width="563"><figcaption></figcaption></figure>

attention weightsを計算します。

<figure><img src="../../images/image (6) (1) (1).png" alt="" width="404"><figcaption></figcaption></figure>

#### Step 3: Context Vectorの計算

> [!TIP]
> 各attention weightを取得して対応するtokenの各次元に掛け、その後すべての次元を加算し、1つのベクトル（context vector）を得るだけです。

**context vector**は、attention weightsを使用した全単語のembeddingの加重和として計算されます。

<figure><img src="../../images/image (16).png" alt="" width="369"><figcaption></figcaption></figure>

各成分を計算します。

- **"Hello"のWeighted Embedding**:

<figure><img src="../../images/image (7) (1) (1).png" alt=""><figcaption></figcaption></figure>

- **"shiny"のWeighted Embedding**:

<figure><img src="../../images/image (8) (1) (1).png" alt=""><figcaption></figcaption></figure>

- **"sun"のWeighted Embedding**:

<figure><img src="../../images/image (9) (1) (1).png" alt=""><figcaption></figcaption></figure>

加重embeddingを合計します。

`context vector=[0.0779+0.2156+0.1057, 0.0504+0.1382+0.1972, 0.1237+0.3983+0.3390]=[0.3992,0.3858,0.8610]`

**このcontext vectorは、文中のすべての単語から情報を取り込み、単語 "shiny" の豊かなembeddingを表します。**

### プロセスの概要

1. **Attention Scoresを計算**: 対象単語のembeddingと、シーケンス内のすべての単語のembeddingとのdot productを使用します。
2. **Scoresを正規化してAttention Weightsを取得**: attention scoresにsoftmax functionを適用し、合計が1になるweightsを取得します。
3. **Context Vectorを計算**: 各単語のembeddingにそのattention weightを掛け、結果を合計します。

## Trainable Weightsを使用したSelf-Attention

実際には、self-attention mechanismsは**trainable weights**を使用して、queries、keys、valuesに最適な表現を学習します。これには3つのweight matricesの導入が含まれます。<sup>[[1]](#references)</sup>

<figure><img src="../../images/image (10) (1) (1).png" alt="" width="239"><figcaption></figcaption></figure>

queryは前述と同様に使用するデータであり、keysとvaluesのmatricesは単にrandom-trainable matricesです。

#### Step 1: Queries、Keys、Valuesの計算

各tokenは、定義されたmatricesによって各次元の値を乗算することで、それぞれ独自のquery、key、value matrixを持ちます。

<figure><img src="../../images/image (11).png" alt="" width="253"><figcaption></figcaption></figure>

これらのmatricesは、元のembeddingsをattentionの計算に適した新しい空間へ変換します。

**例**

以下を仮定します。

- Input dimension `din=3`（embedding size）
- Output dimension `dout=2`（queries、keys、valuesに必要なdimension）

weight matricesを初期化します。
```python
import torch.nn as nn

d_in = 3
d_out = 2

W_query = nn.Parameter(torch.rand(d_in, d_out))
W_key = nn.Parameter(torch.rand(d_in, d_out))
W_value = nn.Parameter(torch.rand(d_in, d_out))
```
クエリ、キー、バリューを計算する：
```python
queries = torch.matmul(inputs, W_query)
keys = torch.matmul(inputs, W_key)
values = torch.matmul(inputs, W_value)
```
#### Step 2: Scaled Dot-Product Attentionを計算する

**Attention Scoresを計算する**

先ほどの例と同様ですが、今回はトークンの各次元の値を使用する代わりに、トークンのkey matrix（各次元を使用してすでに計算済み）を使用します。そのため、各query `qi`​ とkey `kj​` について次のようになります。

<figure><img src="../../images/image (12).png" alt=""><figcaption></figcaption></figure>

**Scoresをスケーリングする**

dot productが大きくなりすぎるのを防ぐため、key dimension `dk`​ の平方根で割ってスケーリングします。

<figure><img src="../../images/image (13).png" alt="" width="295"><figcaption></figcaption></figure>

> [!TIP]
> dot productは非常に大きくなる可能性があるため、dimensionの平方根で割ることで値を調整できます。

**Softmaxを適用してAttention Weightsを取得する:** 最初の例と同様に、すべての値の合計が1になるよう正規化します。

<figure><img src="../../images/image (14).png" alt="" width="295"><figcaption></figcaption></figure>

#### Step 3: Context Vectorsを計算する

最初の例と同様に、すべてのvalue matricesにそれぞれのattention weightを掛けて合計します。

<figure><img src="../../images/image (15).png" alt="" width="328"><figcaption></figcaption></figure>

### Code Example

[https://github.com/rasbt/LLMs-from-scratch/blob/main/ch03/01_main-chapter-code/ch03.ipynb](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch03/01_main-chapter-code/ch03.ipynb) の例を使うと、ここまで説明してきたself-attention機能を実装するclassを確認できます。<sup>[[2]](#references)</sup>
```python
import torch

inputs = torch.tensor(
[[0.43, 0.15, 0.89], # Your     (x^1)
[0.55, 0.87, 0.66], # journey  (x^2)
[0.57, 0.85, 0.64], # starts   (x^3)
[0.22, 0.58, 0.33], # with     (x^4)
[0.77, 0.25, 0.10], # one      (x^5)
[0.05, 0.80, 0.55]] # step     (x^6)
)

import torch.nn as nn
class SelfAttention_v2(nn.Module):

def __init__(self, d_in, d_out, qkv_bias=False):
super().__init__()
self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_key   = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)

def forward(self, x):
keys = self.W_key(x)
queries = self.W_query(x)
values = self.W_value(x)

attn_scores = queries @ keys.T
attn_weights = torch.softmax(attn_scores / keys.shape[-1]**0.5, dim=-1)

context_vec = attn_weights @ values
return context_vec

d_in=3
d_out=2
torch.manual_seed(789)
sa_v2 = SelfAttention_v2(d_in, d_out)
print(sa_v2(inputs))
```
> [!TIP]
> 行列をランダムな値で初期化する代わりに、`nn.Linear` を使用して、すべての重みを学習対象のパラメータとして指定している点に注意してください。

## Causal Attention: 未来の単語を隠す

LLMでは、**次のトークンを予測する**ために、モデルが現在の位置より前に出現するトークンだけを考慮するようにします。**Causal attention**は、**masked attention**とも呼ばれ、未来のトークンにアクセスできないようにattention mechanismを変更することでこれを実現します。<sup>[[1]](#references)</sup>

### Causal Attention Maskの適用

Causal attentionを実装するには、**softmax操作の前**にattention scoresへmaskを適用し、残りの値の合計が1になるようにします。このmaskは未来のトークンのattention scoresを負の無限大に設定し、softmax後にそれらのattention weightsが0になるようにします。

**手順**

1. **Attention Scoresの計算**: 以前と同じです。
2. **Maskの適用**: 対角線より上を負の無限大で埋めた上三角行列を使用します。

```python
mask = torch.triu(torch.ones(seq_len, seq_len), diagonal=1) * float('-inf')
masked_scores = attention_scores + mask
```

3. **Softmaxの適用**: mask済みのscoresを使用してattention weightsを計算します。

```python
attention_weights = torch.softmax(masked_scores, dim=-1)
```

### Dropoutによる追加のAttention WeightsのMask

**過学習を防ぐ**ために、softmax操作の後でattention weightsへ**dropout**を適用できます。Dropoutは、training中に**attention weightsの一部をランダムに0にします**。
```python
dropout = nn.Dropout(p=0.5)
attention_weights = dropout(attention_weights)
```
通常のdropoutは約10～20%です。

### コード例

[https://github.com/rasbt/LLMs-from-scratch/blob/main/ch03/01_main-chapter-code/ch03.ipynb](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch03/01_main-chapter-code/ch03.ipynb)のコード例:<sup>[[2]](#references)</sup>
```python
import torch
import torch.nn as nn

inputs = torch.tensor(
[[0.43, 0.15, 0.89], # Your     (x^1)
[0.55, 0.87, 0.66], # journey  (x^2)
[0.57, 0.85, 0.64], # starts   (x^3)
[0.22, 0.58, 0.33], # with     (x^4)
[0.77, 0.25, 0.10], # one      (x^5)
[0.05, 0.80, 0.55]] # step     (x^6)
)

batch = torch.stack((inputs, inputs), dim=0)
print(batch.shape)

class CausalAttention(nn.Module):

def __init__(self, d_in, d_out, context_length,
dropout, qkv_bias=False):
super().__init__()
self.d_out = d_out
self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_key   = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)
self.dropout = nn.Dropout(dropout)
self.register_buffer('mask', torch.triu(torch.ones(context_length, context_length), diagonal=1)) # New

def forward(self, x):
b, num_tokens, d_in = x.shape
# b is the num of batches
# num_tokens is the number of tokens per batch
# d_in is the dimensions er token

keys = self.W_key(x) # This generates the keys of the tokens
queries = self.W_query(x)
values = self.W_value(x)

attn_scores = queries @ keys.transpose(1, 2) # Moves the third dimension to the second one and the second one to the third one to be able to multiply
attn_scores.masked_fill_(  # New, _ ops are in-place
self.mask.bool()[:num_tokens, :num_tokens], -torch.inf)  # `:num_tokens` to account for cases where the number of tokens in the batch is smaller than the supported context_size
attn_weights = torch.softmax(
attn_scores / keys.shape[-1]**0.5, dim=-1
)
attn_weights = self.dropout(attn_weights)

context_vec = attn_weights @ values
return context_vec

torch.manual_seed(123)

context_length = batch.shape[1]
d_in = 3
d_out = 2
ca = CausalAttention(d_in, d_out, context_length, 0.0)

context_vecs = ca(batch)

print(context_vecs)
print("context_vecs.shape:", context_vecs.shape)
```
## Single-Head AttentionをMulti-Head Attentionに拡張する

実用上、**Multi-Head Attention** は複数のself-attentionヘッドを実行します。各ヘッドには**独自の学習済み射影**があり、それらの出力を結合します。<sup>[[1]](#references)</sup>

### コード例

前の実装はラップしてヘッドごとに1回ずつ呼び出すこともできますが、次の最適化されたバージョンでは、すべてのヘッドをまとめて処理し、高コストなPythonループを回避します。embedding dimensionはヘッド間で均等に分割されます。たとえば、8次元のtoken embeddingを2つのヘッドで処理する場合、各ヘッドには4次元が割り当てられます。したがって、`d_out`は`num_heads`で割り切れなければなりません。<sup>[[2]](#references)</sup>
```python
class MultiHeadAttention(nn.Module):
def __init__(self, d_in, d_out, context_length, dropout, num_heads, qkv_bias=False):
super().__init__()
assert (d_out % num_heads == 0), \
"d_out must be divisible by num_heads"

self.d_out = d_out
self.num_heads = num_heads
self.head_dim = d_out // num_heads # Reduce the projection dim to match desired output dim

self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)
self.out_proj = nn.Linear(d_out, d_out)  # Linear layer to combine head outputs
self.dropout = nn.Dropout(dropout)
self.register_buffer(
"mask",
torch.triu(torch.ones(context_length, context_length),
diagonal=1)
)

def forward(self, x):
b, num_tokens, d_in = x.shape
# b is the num of batches
# num_tokens is the number of tokens per batch
# d_in is the dimensions er token

keys = self.W_key(x) # Shape: (b, num_tokens, d_out)
queries = self.W_query(x)
values = self.W_value(x)

# We implicitly split the matrix by adding a `num_heads` dimension
# Unroll last dim: (b, num_tokens, d_out) -> (b, num_tokens, num_heads, head_dim)
keys = keys.view(b, num_tokens, self.num_heads, self.head_dim)
values = values.view(b, num_tokens, self.num_heads, self.head_dim)
queries = queries.view(b, num_tokens, self.num_heads, self.head_dim)

# Transpose: (b, num_tokens, num_heads, head_dim) -> (b, num_heads, num_tokens, head_dim)
keys = keys.transpose(1, 2)
queries = queries.transpose(1, 2)
values = values.transpose(1, 2)

# Compute scaled dot-product attention (aka self-attention) with a causal mask
attn_scores = queries @ keys.transpose(2, 3)  # Dot product for each head

# Original mask truncated to the number of tokens and converted to boolean
mask_bool = self.mask.bool()[:num_tokens, :num_tokens]

# Use the mask to fill attention scores
attn_scores.masked_fill_(mask_bool, -torch.inf)

attn_weights = torch.softmax(attn_scores / keys.shape[-1]**0.5, dim=-1)
attn_weights = self.dropout(attn_weights)

# Shape: (b, num_tokens, num_heads, head_dim)
context_vec = (attn_weights @ values).transpose(1, 2)

# Combine heads, where self.d_out = self.num_heads * self.head_dim
context_vec = context_vec.contiguous().view(b, num_tokens, self.d_out)
context_vec = self.out_proj(context_vec) # optional projection

return context_vec

torch.manual_seed(123)

batch_size, context_length, d_in = batch.shape
d_out = 2
mha = MultiHeadAttention(d_in, d_out, context_length, 0.0, num_heads=2)

context_vecs = mha(batch)

print(context_vecs)
print("context_vecs.shape:", context_vecs.shape)

```
別のコンパクトで効率的な実装には、PyTorch の [`torch.nn.MultiheadAttention`](https://pytorch.org/docs/stable/generated/torch.nn.MultiheadAttention.html) クラスを使用できます。

> [!TIP]
> トークンの各ヘッドに全次元を確認させるのではなく、トークンの次元をヘッド間で分割する方が優れている理由についての ChatGPT の簡潔な回答:
>
> 各ヘッドがすべての埋め込み次元を処理できるようにすると、各ヘッドが完全な情報にアクセスできるため有利に思えるかもしれません。しかし、標準的な手法では、**埋め込み次元をヘッド間で分割**します。この方法は、計算効率とモデル性能のバランスを取り、各ヘッドが多様な表現を学習することを促します。そのため、各ヘッドがすべての次元を確認するよりも、埋め込み次元を分割する方が一般的に好まれます。

## References

- [1] [大規模言語モデルを構築する（ゼロから） - Manning](https://www.manning.com/books/build-a-large-language-model-from-scratch)
- [2] [LLMs-from-scratch - 第3章のコード (rasbt/LLMs-from-scratch)](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch03/01_main-chapter-code/ch03.ipynb)
{{#include ../../banners/hacktricks-training.md}}
