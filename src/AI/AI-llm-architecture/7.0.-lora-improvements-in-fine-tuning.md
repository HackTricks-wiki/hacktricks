# 7.0. fine-tuningにおけるLoRAの改善

{{#include ../../banners/hacktricks-training.md}}

## LoRAの改善

> [!TIP]
> LoRAは、pretrained modelのfine-tuningに必要なtrainable parameter数とoptimizer memoryを大幅に削減できます。

LoRAを使用すると、モデルの**小さな部分**だけを変更することで、**large models**を効率的にfine-tuningできます。trainする必要があるparameter数を減らし、**memory**と**computational resources**を節約します。これは次の理由によるものです。<sup>[[1]](#references)</sup>

1. **trainable parameter数を削減**: LoRAは元のweight `W`をfreezeし、加算的なupdateを、2つのtrainableなlow-rank matrixの積として表現します。一般的には `ΔW = B A` とし、rank `r`はinput/output dimensionsよりもはるかに小さくします。<sup>[[3]](#references)</sup>

1. これは、layer（matrix）の完全なweight updateを計算する代わりに、2つの小さなmatrixの積として近似し、計算するupdateを削減するためです:\

<figure><img src="../../images/image (9) (1).png" alt=""><figcaption></figcaption></figure>

2. **元のmodel weightsを変更せずに維持**: 最適化されるのはadapter matricesだけです。weightsをfreezeすることで、base checkpointが直接変更されるのを防ぎます。ただし、inference時にはadapterの挙動によって知識がoverrideされたり、劣化したりする可能性があります。
3. **タスク固有の効率的なFine-Tuning**: モデルを**new task**に適応させたい場合、モデルの残りの部分をそのままにして、**small LoRA matrices**（AとB）だけをtrainできます。これはモデル全体を再trainingするよりも**はるかに効率的**です。
4. **Storage Efficiency**: fine-tuning後、各タスク用に**モデル全体**を保存する代わりに、モデル全体と比べて非常に小さい**LoRA matrices**だけを保存すれば済みます。これにより、storageを使いすぎることなく、モデルを多くのタスクに適応させやすくなります。

以下の最小実装は、引用されたnotebookを適応したものです。adapterがno-opとして開始するように`B`をzeroで初期化し、LoRAで標準的な`alpha / rank` scalingを使用します。このcodeではfactorをinput-major shapes（`A`は`in_dim x rank`、`B`は`rank x out_dim`）で保存するため、forward pathは`xAB`として記述されています。これは、paperでの`BA` notationと同等のtransposed-shape表現です。<sup>[[2]](#references)[[3]](#references)</sup>
```python
import math
import torch

# Create the LoRA layer with the 2 matrices and the alpha
class LoRALayer(torch.nn.Module):
def __init__(self, in_dim, out_dim, rank, alpha):
super().__init__()
self.A = torch.nn.Parameter(torch.empty(in_dim, rank))
torch.nn.init.kaiming_uniform_(self.A, a=math.sqrt(5))  # similar to standard weight initialization
self.B = torch.nn.Parameter(torch.zeros(rank, out_dim))
self.scaling = alpha / rank

def forward(self, x):
x = self.scaling * (x @ self.A @ self.B)
return x

# Combine it with the linear layer
class LinearWithLoRA(torch.nn.Module):
def __init__(self, linear, rank, alpha):
super().__init__()
self.linear = linear
self.lora = LoRALayer(
linear.in_features, linear.out_features, rank, alpha
)

def forward(self, x):
return self.linear(x) + self.lora(x)

# Replace linear layers with LoRA ones
def replace_linear_with_lora(model, rank, alpha):
for name, module in model.named_children():
if isinstance(module, torch.nn.Linear):
# Replace the Linear layer with LinearWithLoRA
setattr(model, name, LinearWithLoRA(module, rank, alpha))
else:
# Recursively apply the same function to child modules
replace_linear_with_lora(module, rank, alpha)
```
## References

- [1] [大規模言語モデルをゼロから構築する - Manning](https://www.manning.com/books/build-a-large-language-model-from-scratch)
- [2] [ゼロからのLLM - 付録E：LoRAによるパラメータ効率の高いfine-tuning](https://github.com/rasbt/LLMs-from-scratch/blob/main/appendix-E/01_main-chapter-code/appendix-E.ipynb)
- [3] [LoRA：大規模言語モデルの低ランク適応](https://arxiv.org/abs/2106.09685)
{{#include ../../banners/hacktricks-training.md}}
