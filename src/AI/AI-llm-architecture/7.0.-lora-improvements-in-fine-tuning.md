# 7.0. Покращення LoRA у дообученні

{{#include ../../banners/hacktricks-training.md}}

## Покращення LoRA

> [!TIP]
> Використання **LoRA значно зменшує обчислення**, необхідні для **дообучення** вже навчених моделей.

LoRA дає змогу ефективно дообучати **великі моделі**, змінюючи лише **невелику частину** моделі. Це зменшує кількість параметрів, які потрібно навчати, заощаджуючи **пам'ять** і **обчислювальні ресурси**. Це відбувається тому, що:<sup>[[1]](#references)</sup>

1. **Зменшує кількість параметрів, що навчаються**: Замість оновлення всієї матриці ваг моделі LoRA **розділяє** матрицю ваг на дві менші матриці (які називаються **A** і **B**). Це робить навчання **швидшим** і потребує **менше пам'яті**, оскільки потрібно оновлювати менше параметрів.<sup>[[3]](#references)</sup>

1. Це відбувається тому, що замість обчислення повного оновлення ваг шару (матриці) воно апроксимується добутком 2 менших матриць, що зменшує обсяг обчислень для оновлення:\

<figure><img src="../../images/image (9) (1).png" alt=""><figcaption></figcaption></figure>

2. **Зберігає незмінними ваги оригінальної моделі**: LoRA дає змогу залишити ваги оригінальної моделі без змін і оновлює лише **нові малі матриці** (A і B). Це корисно, оскільки початкові знання моделі зберігаються, а змінюється лише те, що необхідно.
3. **Ефективне дообучення для конкретного завдання**: Коли потрібно адаптувати модель до **нового завдання**, можна навчати лише **малі матриці LoRA** (A і B), залишаючи решту моделі без змін. Це **набагато ефективніше**, ніж повторне навчання всієї моделі.
4. **Ефективне використання сховища**: Після дообучення замість збереження **цілої нової моделі** для кожного завдання потрібно зберігати лише **матриці LoRA**, які дуже малі порівняно з усією моделлю. Це спрощує адаптацію моделі до багатьох завдань без надмірного використання сховища.

Щоб реалізувати LoraLayers замість Linear під час дообучення, тут запропоновано такий код [https://github.com/rasbt/LLMs-from-scratch/blob/main/appendix-E/01_main-chapter-code/appendix-E.ipynb](https://github.com/rasbt/LLMs-from-scratch/blob/main/appendix-E/01_main-chapter-code/appendix-E.ipynb):<sup>[[2]](#references)</sup>
```python
import math

# Create the LoRA layer with the 2 matrices and the alpha
class LoRALayer(torch.nn.Module):
def __init__(self, in_dim, out_dim, rank, alpha):
super().__init__()
self.A = torch.nn.Parameter(torch.empty(in_dim, rank))
torch.nn.init.kaiming_uniform_(self.A, a=math.sqrt(5))  # similar to standard weight initialization
self.B = torch.nn.Parameter(torch.zeros(rank, out_dim))
self.alpha = alpha

def forward(self, x):
x = self.alpha * (x @ self.A @ self.B)
return x

# Combine it with the linear layer
class LinearWithLoRA(torch.nn.Module):
def __init__(self, linear, rank, alpha):
super().__init__()
self.linear = linear
self.lora = LoRALayer(
linear.in_features, linear.out_features, rank, alpha
)

def forward(self, x):
return self.linear(x) + self.lora(x)

# Replace linear layers with LoRA ones
def replace_linear_with_lora(model, rank, alpha):
for name, module in model.named_children():
if isinstance(module, torch.nn.Linear):
# Replace the Linear layer with LinearWithLoRA
setattr(model, name, LinearWithLoRA(module, rank, alpha))
else:
# Recursively apply the same function to child modules
replace_linear_with_lora(module, rank, alpha)
```
## Посилання

- [1] [Створення великої мовної моделі (з нуля) - Manning](https://www.manning.com/books/build-a-large-language-model-from-scratch)
- [2] [LLMs-from-scratch - Додаток E: Parameter-efficient fine-tuning with LoRA](https://github.com/rasbt/LLMs-from-scratch/blob/main/appendix-E/01_main-chapter-code/appendix-E.ipynb)
- [3] [LoRA: низькорангова адаптація великих мовних моделей](https://arxiv.org/abs/2106.09685)

{{#include ../../banners/hacktricks-training.md}}
