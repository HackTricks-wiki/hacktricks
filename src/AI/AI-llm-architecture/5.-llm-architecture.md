# 5. LLM-Architektur

{{#include ../../banners/hacktricks-training.md}}

## LLM-Architektur

> [!TIP]
> Das Ziel dieser fünften Phase ist sehr einfach: **Die Architektur des vollständigen LLM entwickeln**. Alles zusammenfügen, alle Layer anwenden und alle Funktionen erstellen, um Text zu generieren oder Text in IDs und zurück umzuwandeln.
>
> Diese Architektur wird sowohl für das Training als auch für die Vorhersage von Text nach dem Training verwendet.

Beispiel einer LLM-Architektur von [https://github.com/rasbt/LLMs-from-scratch/blob/main/ch04/01_main-chapter-code/ch04.ipynb](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch04/01_main-chapter-code/ch04.ipynb):<sup>[[1]](#references)</sup>

Eine Darstellung auf hoher Ebene ist hier zu sehen:

<figure><img src="../../images/image (3) (1) (1) (1).png" alt="" width="563"><figcaption><p><a href="https://camo.githubusercontent.com/6c8c392f72d5b9e86c94aeb9470beab435b888d24135926f1746eb88e0cc18fb/68747470733a2f2f73656261737469616e72617363686b612e636f6d2f696d616765732f4c4c4d732d66726f6d2d736372617463682d696d616765732f636830345f636f6d707265737365645f696d616765732f31332e776562703f31">https://camo.githubusercontent.com/6c8c392f72d5b9e86c94aeb9470beab435b888d24135926f1746eb88e0cc18fb/68747470733a2f2f73656261737469616e72617363686b612e636f6d2f696d616765732f4c4c4d732d66726f6d2d736372617463682d696d616765732f636830345f636f6d707265737365645f696d616765732f31332e776562703f31</a></p></figcaption></figure>

1. **Input (tokenisierter Text)**: Der Prozess beginnt mit tokenisiertem Text, der in numerische Darstellungen umgewandelt wird.
2. **Token-Embedding- und Positions-Embedding-Layer**: Der tokenisierte Text wird durch einen **Token-Embedding**-Layer und einen **Positions-Embedding**-Layer geleitet. Letzterer erfasst die Position der Tokens in einer Sequenz, was für das Verständnis der Wortreihenfolge entscheidend ist.
3. **Transformer-Blöcke**: Das Modell enthält **12 Transformer-Blöcke**, die jeweils aus mehreren Layern bestehen. Diese Blöcke wiederholen die folgende Sequenz:
- **Masked Multi-Head Attention**: Ermöglicht es dem Modell, sich gleichzeitig auf verschiedene Teile des Eingabetextes zu konzentrieren.
- **Layer Normalization**: Ein Normalisierungsschritt zur Stabilisierung und Verbesserung des Trainings.
- **Feed Forward Layer**: Verarbeitet die Informationen aus dem Attention-Layer und erstellt Vorhersagen über das nächste Token.
- **Dropout Layers**: Diese Layer verhindern Overfitting, indem während des Trainings zufällig Einheiten entfernt werden.
4. **Finaler Output-Layer**: Das Modell gibt einen **4x50.257-dimensionalen Tensor** aus, wobei **50.257** die Größe des Vokabulars darstellt. Jede Zeile dieses Tensors entspricht einem Vektor, den das Modell zur Vorhersage des nächsten Wortes in der Sequenz verwendet.
5. **Ziel**: Das Ziel besteht darin, diese Embeddings wieder in Text umzuwandeln. Konkret wird die letzte Zeile des Outputs verwendet, um das nächste Wort zu generieren, das in diesem Diagramm als "forward" dargestellt wird.

### Code-Darstellung
```python
import torch
import torch.nn as nn
import tiktoken

class GELU(nn.Module):
def __init__(self):
super().__init__()

def forward(self, x):
return 0.5 * x * (1 + torch.tanh(
torch.sqrt(torch.tensor(2.0 / torch.pi)) *
(x + 0.044715 * torch.pow(x, 3))
))

class FeedForward(nn.Module):
def __init__(self, cfg):
super().__init__()
self.layers = nn.Sequential(
nn.Linear(cfg["emb_dim"], 4 * cfg["emb_dim"]),
GELU(),
nn.Linear(4 * cfg["emb_dim"], cfg["emb_dim"]),
)

def forward(self, x):
return self.layers(x)

class MultiHeadAttention(nn.Module):
def __init__(self, d_in, d_out, context_length, dropout, num_heads, qkv_bias=False):
super().__init__()
assert d_out % num_heads == 0, "d_out must be divisible by num_heads"

self.d_out = d_out
self.num_heads = num_heads
self.head_dim = d_out // num_heads # Reduce the projection dim to match desired output dim

self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)
self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)
self.out_proj = nn.Linear(d_out, d_out)  # Linear layer to combine head outputs
self.dropout = nn.Dropout(dropout)
self.register_buffer('mask', torch.triu(torch.ones(context_length, context_length), diagonal=1))

def forward(self, x):
b, num_tokens, d_in = x.shape

keys = self.W_key(x) # Shape: (b, num_tokens, d_out)
queries = self.W_query(x)
values = self.W_value(x)

# We implicitly split the matrix by adding a `num_heads` dimension
# Unroll last dim: (b, num_tokens, d_out) -> (b, num_tokens, num_heads, head_dim)
keys = keys.view(b, num_tokens, self.num_heads, self.head_dim)
values = values.view(b, num_tokens, self.num_heads, self.head_dim)
queries = queries.view(b, num_tokens, self.num_heads, self.head_dim)

# Transpose: (b, num_tokens, num_heads, head_dim) -> (b, num_heads, num_tokens, head_dim)
keys = keys.transpose(1, 2)
queries = queries.transpose(1, 2)
values = values.transpose(1, 2)

# Compute scaled dot-product attention (aka self-attention) with a causal mask
attn_scores = queries @ keys.transpose(2, 3)  # Dot product for each head

# Original mask truncated to the number of tokens and converted to boolean
mask_bool = self.mask.bool()[:num_tokens, :num_tokens]

# Use the mask to fill attention scores
attn_scores.masked_fill_(mask_bool, -torch.inf)

attn_weights = torch.softmax(attn_scores / keys.shape[-1]**0.5, dim=-1)
attn_weights = self.dropout(attn_weights)

# Shape: (b, num_tokens, num_heads, head_dim)
context_vec = (attn_weights @ values).transpose(1, 2)

# Combine heads, where self.d_out = self.num_heads * self.head_dim
context_vec = context_vec.contiguous().view(b, num_tokens, self.d_out)
context_vec = self.out_proj(context_vec) # optional projection

return context_vec

class LayerNorm(nn.Module):
def __init__(self, emb_dim):
super().__init__()
self.eps = 1e-5
self.scale = nn.Parameter(torch.ones(emb_dim))
self.shift = nn.Parameter(torch.zeros(emb_dim))

def forward(self, x):
mean = x.mean(dim=-1, keepdim=True)
var = x.var(dim=-1, keepdim=True, unbiased=False)
norm_x = (x - mean) / torch.sqrt(var + self.eps)
return self.scale * norm_x + self.shift

class TransformerBlock(nn.Module):
def __init__(self, cfg):
super().__init__()
self.att = MultiHeadAttention(
d_in=cfg["emb_dim"],
d_out=cfg["emb_dim"],
context_length=cfg["context_length"],
num_heads=cfg["n_heads"],
dropout=cfg["drop_rate"],
qkv_bias=cfg["qkv_bias"])
self.ff = FeedForward(cfg)
self.norm1 = LayerNorm(cfg["emb_dim"])
self.norm2 = LayerNorm(cfg["emb_dim"])
self.drop_shortcut = nn.Dropout(cfg["drop_rate"])

def forward(self, x):
# Shortcut connection for attention block
shortcut = x
x = self.norm1(x)
x = self.att(x)  # Shape [batch_size, num_tokens, emb_size]
x = self.drop_shortcut(x)
x = x + shortcut  # Add the original input back

# Shortcut connection for feed forward block
shortcut = x
x = self.norm2(x)
x = self.ff(x)
x = self.drop_shortcut(x)
x = x + shortcut  # Add the original input back

return x


class GPTModel(nn.Module):
def __init__(self, cfg):
super().__init__()
self.tok_emb = nn.Embedding(cfg["vocab_size"], cfg["emb_dim"])
self.pos_emb = nn.Embedding(cfg["context_length"], cfg["emb_dim"])
self.drop_emb = nn.Dropout(cfg["drop_rate"])

self.trf_blocks = nn.Sequential(
*[TransformerBlock(cfg) for _ in range(cfg["n_layers"])])

self.final_norm = LayerNorm(cfg["emb_dim"])
self.out_head = nn.Linear(
cfg["emb_dim"], cfg["vocab_size"], bias=False
)

def forward(self, in_idx):
batch_size, seq_len = in_idx.shape
tok_embeds = self.tok_emb(in_idx)
pos_embeds = self.pos_emb(torch.arange(seq_len, device=in_idx.device))
x = tok_embeds + pos_embeds  # Shape [batch_size, num_tokens, emb_size]
x = self.drop_emb(x)
x = self.trf_blocks(x)
x = self.final_norm(x)
logits = self.out_head(x)
return logits

GPT_CONFIG_124M = {
"vocab_size": 50257,    # Vocabulary size
"context_length": 1024, # Context length
"emb_dim": 768,         # Embedding dimension
"n_heads": 12,          # Number of attention heads
"n_layers": 12,         # Number of layers
"drop_rate": 0.1,       # Dropout rate
"qkv_bias": False       # Query-Key-Value bias
}

torch.manual_seed(123)
model = GPTModel(GPT_CONFIG_124M)
out = model(batch)
print("Input batch:\n", batch)
print("\nOutput shape:", out.shape)
print(out)
```
Erklären wir es Schritt für Schritt:<sup>[[2]](#references)</sup>

### **GELU-Aktivierungsfunktion**
```python
# From https://github.com/rasbt/LLMs-from-scratch/tree/main/ch04
class GELU(nn.Module):
def __init__(self):
super().__init__()

def forward(self, x):
return 0.5 * x * (1 + torch.tanh(
torch.sqrt(torch.tensor(2.0 / torch.pi)) *
(x + 0.044715 * torch.pow(x, 3))
))
```
#### **Zweck und Funktionalität**

- **GELU (Gaussian Error Linear Unit):** Eine Aktivierungsfunktion, die Nichtlinearität in das Modell einführt.
- **Glatte Aktivierung:** Anders als ReLU, das negative Eingaben auf null setzt, ordnet GELU Eingaben auf sanfte Weise Ausgaben zu und ermöglicht kleine, von null verschiedene Werte für negative Eingaben.
- **Mathematische Definition:**

<figure><img src="../../images/image (2) (1) (1) (1).png" alt=""><figcaption></figcaption></figure>

> [!TIP]
> Das Ziel der Verwendung dieser Funktion nach linearen Schichten innerhalb der FeedForward-Schicht besteht darin, die linearen Daten in nichtlineare Daten umzuwandeln, damit das Modell komplexe, nichtlineare Beziehungen erlernen kann.

### **FeedForward-Neuronales Netzwerk**

_Die Formen wurden als Kommentare hinzugefügt, um die Formen der Matrizen besser zu verstehen:_
```python
# From https://github.com/rasbt/LLMs-from-scratch/tree/main/ch04
class FeedForward(nn.Module):
def __init__(self, cfg):
super().__init__()
self.layers = nn.Sequential(
nn.Linear(cfg["emb_dim"], 4 * cfg["emb_dim"]),
GELU(),
nn.Linear(4 * cfg["emb_dim"], cfg["emb_dim"]),
)

def forward(self, x):
# x shape: (batch_size, seq_len, emb_dim)

x = self.layers[0](x)# x shape: (batch_size, seq_len, 4 * emb_dim)
x = self.layers[1](x) # x shape remains: (batch_size, seq_len, 4 * emb_dim)
x = self.layers[2](x) # x shape: (batch_size, seq_len, emb_dim)
return x  # Output shape: (batch_size, seq_len, emb_dim)
```
#### **Zweck und Funktionalität**

- **Position-wise FeedForward Network:** Wendet an jeder Position separat und identisch ein zweischichtiges vollständig verbundenes Netzwerk an.
- **Details der Schichten:**
- **Erste lineare Schicht:** Erweitert die Dimensionalität von `emb_dim` auf `4 * emb_dim`.
- **GELU-Aktivierung:** Wendet Nichtlinearität an.
- **Zweite lineare Schicht:** Reduziert die Dimensionalität wieder auf `emb_dim`.

> [!TIP]
> Wie du sehen kannst, verwendet das Feed Forward Network 3 Schichten. Die erste ist eine lineare Schicht, die die Dimensionen mithilfe linearer Gewichte (innerhalb des Modells zu trainierende Parameter) mit dem Faktor 4 multipliziert. Anschließend wird die GELU-Funktion auf all diese Dimensionen angewendet, um nichtlineare Variationen zu erzeugen und umfangreichere Repräsentationen zu erfassen. Schließlich wird eine weitere lineare Schicht verwendet, um zur ursprünglichen Dimensionsgröße zurückzukehren.

### **Multi-Head Attention Mechanism**

Dies wurde bereits in einem früheren Abschnitt erklärt.

#### **Zweck und Funktionalität**

- **Multi-Head Self-Attention:** Ermöglicht es dem Modell, sich beim Encodieren eines Tokens auf verschiedene Positionen innerhalb der Eingabesequenz zu konzentrieren.
- **Hauptkomponenten:**
- **Queries, Keys, Values:** Lineare Projektionen der Eingabe, die zur Berechnung der Attention Scores verwendet werden.
- **Heads:** Mehrere parallel ausgeführte Attention-Mechanismen (`num_heads`), jeweils mit einer reduzierten Dimension (`head_dim`).
- **Attention Scores:** Werden als Skalarprodukt von Queries und Keys berechnet, skaliert und maskiert.
- **Masking:** Eine kausale Maske wird angewendet, um zu verhindern, dass das Modell auf zukünftige Tokens zugreift (wichtig für autoregressive Modelle wie GPT).
- **Attention Weights:** Softmax der maskierten und skalierten Attention Scores.
- **Context Vector:** Gewichtete Summe der Values entsprechend den Attention Weights.
- **Output Projection:** Lineare Schicht zur Kombination der Outputs aller Heads.

> [!TIP]
> Das Ziel dieses Netzwerks besteht darin, die Beziehungen zwischen Tokens im selben Kontext zu ermitteln. Außerdem werden die Tokens in verschiedene Heads aufgeteilt, um Overfitting zu verhindern, obwohl die schließlich pro Head ermittelten Beziehungen am Ende dieses Netzwerks kombiniert werden.
>
> Außerdem wird während des Trainings eine **causale Maske** angewendet, damit spätere Tokens bei der Untersuchung der spezifischen Beziehungen zu einem Token nicht berücksichtigt werden. Zusätzlich wird etwas **dropout** angewendet, um **Overfitting zu verhindern**.

### **Schicht** Normalisierung
```python
# From https://github.com/rasbt/LLMs-from-scratch/tree/main/ch04
class LayerNorm(nn.Module):
def __init__(self, emb_dim):
super().__init__()
self.eps = 1e-5 # Prevent division by zero during normalization.
self.scale = nn.Parameter(torch.ones(emb_dim))
self.shift = nn.Parameter(torch.zeros(emb_dim))

def forward(self, x):
mean = x.mean(dim=-1, keepdim=True)
var = x.var(dim=-1, keepdim=True, unbiased=False)
norm_x = (x - mean) / torch.sqrt(var + self.eps)
return self.scale * norm_x + self.shift
```
#### **Zweck und Funktionalität**

- **Layer-Normalisierung:** Eine Technik zur Normalisierung der Eingaben über die Features (Embedding-Dimensionen) für jedes einzelne Beispiel in einem Batch.
- **Komponenten:**
- **`eps`:** Eine kleine Konstante (`1e-5`), die zur Varianz addiert wird, um während der Normalisierung eine Division durch null zu verhindern.
- **`scale` und `shift`:** Lernbare Parameter (`nn.Parameter`), mit denen das Modell die normalisierte Ausgabe skalieren und verschieben kann. Sie werden jeweils mit Einsen und Nullen initialisiert.
- **Normalisierungsprozess:**
- **Mittelwert berechnen (`mean`):** Berechnet den Mittelwert der Eingabe `x` über die Embedding-Dimension (`dim=-1`) und behält die Dimension für Broadcasting bei (`keepdim=True`).
- **Varianz berechnen (`var`):** Berechnet die Varianz von `x` über die Embedding-Dimension und behält ebenfalls die Dimension bei. Der Parameter `unbiased=False` stellt sicher, dass die Varianz mit dem verzerrten Schätzer berechnet wird (Division durch `N` statt durch `N-1`), was bei der Normalisierung über Features statt über Samples angemessen ist.
- **Normalisieren (`norm_x`):** Subtrahiert den Mittelwert von `x` und dividiert durch die Quadratwurzel aus der Varianz plus `eps`.
- **Skalieren und Verschieben:** Wendet die lernbaren Parameter `scale` und `shift` auf die normalisierte Ausgabe an.

> [!TIP]
> Ziel ist es, über alle Dimensionen desselben Tokens einen Mittelwert von 0 und eine Varianz von 1 zu erreichen. Dadurch soll das **Training tiefer neuronaler Netzwerke stabilisiert** werden, indem der interne Kovariaten-Shift reduziert wird. Dieser bezeichnet die Veränderung der Verteilung von Netzwerkaktivierungen aufgrund der Aktualisierung von Parametern während des Trainings.

### **Transformer-Block**

_Die Shapes wurden als Kommentare hinzugefügt, um die Shapes der Matrizen besser zu verstehen:_
```python
# From https://github.com/rasbt/LLMs-from-scratch/tree/main/ch04

class TransformerBlock(nn.Module):
def __init__(self, cfg):
super().__init__()
self.att = MultiHeadAttention(
d_in=cfg["emb_dim"],
d_out=cfg["emb_dim"],
context_length=cfg["context_length"],
num_heads=cfg["n_heads"],
dropout=cfg["drop_rate"],
qkv_bias=cfg["qkv_bias"]
)
self.ff = FeedForward(cfg)
self.norm1 = LayerNorm(cfg["emb_dim"])
self.norm2 = LayerNorm(cfg["emb_dim"])
self.drop_shortcut = nn.Dropout(cfg["drop_rate"])

def forward(self, x):
# x shape: (batch_size, seq_len, emb_dim)

# Shortcut connection for attention block
shortcut = x  # shape: (batch_size, seq_len, emb_dim)
x = self.norm1(x)  # shape remains (batch_size, seq_len, emb_dim)
x = self.att(x)    # shape: (batch_size, seq_len, emb_dim)
x = self.drop_shortcut(x)  # shape remains (batch_size, seq_len, emb_dim)
x = x + shortcut   # shape: (batch_size, seq_len, emb_dim)

# Shortcut connection for feedforward block
shortcut = x       # shape: (batch_size, seq_len, emb_dim)
x = self.norm2(x)  # shape remains (batch_size, seq_len, emb_dim)
x = self.ff(x)     # shape: (batch_size, seq_len, emb_dim)
x = self.drop_shortcut(x)  # shape remains (batch_size, seq_len, emb_dim)
x = x + shortcut   # shape: (batch_size, seq_len, emb_dim)

return x  # Output shape: (batch_size, seq_len, emb_dim)

```
#### **Zweck und Funktionalität**

- **Zusammensetzung der Layer:** Kombiniert Multi-Head Attention, Feedforward-Netzwerk, Layer Normalization und Residual Connections.
- **Layer Normalization:** Wird vor den Attention- und Feedforward-Layern angewendet, um ein stabiles Training zu gewährleisten.
- **Residual Connections (Shortcuts):** Addieren den Input eines Layers zu dessen Output, um den Gradientenfluss zu verbessern und das Training tiefer Netzwerke zu ermöglichen.
- **Dropout:** Wird nach den Attention- und Feedforward-Layern zur Regularisierung angewendet.

#### **Funktionalität Schritt für Schritt**

1. **Erster Residual-Pfad (Self-Attention):**
- **Input (`shortcut`):** Speichert den ursprünglichen Input für die Residual Connection.
- **Layer Norm (`norm1`):** Normalisiert den Input.
- **Multi-Head Attention (`att`):** Wendet Self-Attention an.
- **Dropout (`drop_shortcut`):** Wendet Dropout zur Regularisierung an.
- **Residual hinzufügen (`x + shortcut`):** Kombiniert den ursprünglichen Input mit dem Ergebnis.
2. **Zweiter Residual-Pfad (FeedForward):**
- **Input (`shortcut`):** Speichert den aktualisierten Input für die nächste Residual Connection.
- **Layer Norm (`norm2`):** Normalisiert den Input.
- **FeedForward-Netzwerk (`ff`):** Wendet die Feedforward-Transformation an.
- **Dropout (`drop_shortcut`):** Wendet Dropout an.
- **Residual hinzufügen (`x + shortcut`):** Kombiniert das Ergebnis mit dem Input des ersten Residual-Pfads.

> [!TIP]
> Der Transformer-Block fasst alle Netzwerke zusammen und wendet **Normalisierung** und **Dropouts** an, um die Trainingsstabilität und die Ergebnisse zu verbessern.\
> Beachte, dass Dropouts nach der Verwendung jedes Netzwerks durchgeführt werden, während die Normalisierung vorher angewendet wird.
>
> Außerdem verwendet er Shortcuts, die darin bestehen, **den Output eines Netzwerks zu dessen Input zu addieren**. Dies hilft, das Vanishing-Gradient-Problem zu verhindern, indem sichergestellt wird, dass die anfänglichen Layer "ebenso stark" wie die letzten beitragen.

### **GPTModel**

_Die Shapes wurden als Kommentare hinzugefügt, um die Shapes der Matrizen besser zu verstehen:_
```python
# From https://github.com/rasbt/LLMs-from-scratch/tree/main/ch04
class GPTModel(nn.Module):
def __init__(self, cfg):
super().__init__()
self.tok_emb = nn.Embedding(cfg["vocab_size"], cfg["emb_dim"])
# shape: (vocab_size, emb_dim)

self.pos_emb = nn.Embedding(cfg["context_length"], cfg["emb_dim"])
# shape: (context_length, emb_dim)

self.drop_emb = nn.Dropout(cfg["drop_rate"])

self.trf_blocks = nn.Sequential(
*[TransformerBlock(cfg) for _ in range(cfg["n_layers"])]
)
# Stack of TransformerBlocks

self.final_norm = LayerNorm(cfg["emb_dim"])
self.out_head = nn.Linear(cfg["emb_dim"], cfg["vocab_size"], bias=False)
# shape: (emb_dim, vocab_size)

def forward(self, in_idx):
# in_idx shape: (batch_size, seq_len)
batch_size, seq_len = in_idx.shape

# Token embeddings
tok_embeds = self.tok_emb(in_idx)
# shape: (batch_size, seq_len, emb_dim)

# Positional embeddings
pos_indices = torch.arange(seq_len, device=in_idx.device)
# shape: (seq_len,)
pos_embeds = self.pos_emb(pos_indices)
# shape: (seq_len, emb_dim)

# Add token and positional embeddings
x = tok_embeds + pos_embeds  # Broadcasting over batch dimension
# x shape: (batch_size, seq_len, emb_dim)

x = self.drop_emb(x)  # Dropout applied
# x shape remains: (batch_size, seq_len, emb_dim)

x = self.trf_blocks(x)  # Pass through Transformer blocks
# x shape remains: (batch_size, seq_len, emb_dim)

x = self.final_norm(x)  # Final LayerNorm
# x shape remains: (batch_size, seq_len, emb_dim)

logits = self.out_head(x)  # Project to vocabulary size
# logits shape: (batch_size, seq_len, vocab_size)

return logits  # Output shape: (batch_size, seq_len, vocab_size)
```
#### **Zweck und Funktionalität**

- **Embedding Layers:**
- **Token Embeddings (`tok_emb`):** Wandelt Token-Indizes in Embeddings um. Zur Erinnerung: Dies sind die Gewichte, die jeder Dimension jedes Tokens im Vokabular zugewiesen werden.
- **Positional Embeddings (`pos_emb`):** Fügt den Embeddings Positionsinformationen hinzu, um die Reihenfolge der Tokens zu erfassen. Zur Erinnerung: Dies sind die Gewichte, die einem Token entsprechend seiner Position im Text zugewiesen werden.
- **Dropout (`drop_emb`):** Wird zur Regularisierung auf die Embeddings angewendet.
- **Transformer Blocks (`trf_blocks`):** Stapel aus `n_layers` Transformer-Blöcken zur Verarbeitung der Embeddings.
- **Final Normalization (`final_norm`):** Layer-Normalisierung vor der Output-Schicht.
- **Output Layer (`out_head`):** Projiziert die finalen Hidden States auf die Größe des Vokabulars, um Logits für die Vorhersage zu erzeugen.

> [!TIP]
> Das Ziel dieser Klasse besteht darin, alle anderen genannten Netzwerke zu verwenden, um **das nächste Token in einer Sequenz vorherzusagen**, was für Aufgaben wie die Textgenerierung grundlegend ist.
>
> Beachte, dass sie **so viele Transformer-Blöcke verwendet, wie angegeben** und dass jeder Transformer-Block ein Multi-Head-Attention-Netzwerk, ein Feed-Forward-Netzwerk und mehrere Normalisierungen verwendet. Wenn also 12 Transformer-Blöcke verwendet werden, multipliziere dies mit 12.
>
> Außerdem wird eine **Normalisierungsschicht** **vor** dem **Output** hinzugefügt, und am Ende wird eine finale lineare Schicht angewendet, um die Ergebnisse mit den korrekten Dimensionen zu erhalten. Beachte, dass jeder finale Vektor die Größe des verwendeten Vokabulars hat. Dies liegt daran, dass für jedes mögliche Token innerhalb des Vokabulars eine Wahrscheinlichkeit ermittelt werden soll.

## Anzahl der zu trainierenden Parameter

Nachdem die GPT-Struktur definiert wurde, kann die Anzahl der zu trainierenden Parameter ermittelt werden:<sup>[[1]](#references)</sup>
```python
GPT_CONFIG_124M = {
"vocab_size": 50257,    # Vocabulary size
"context_length": 1024, # Context length
"emb_dim": 768,         # Embedding dimension
"n_heads": 12,          # Number of attention heads
"n_layers": 12,         # Number of layers
"drop_rate": 0.1,       # Dropout rate
"qkv_bias": False       # Query-Key-Value bias
}

model = GPTModel(GPT_CONFIG_124M)
total_params = sum(p.numel() for p in model.parameters())
print(f"Total number of parameters: {total_params:,}")
# Total number of parameters: 163,009,536
```
### **Schritt-für-Schritt-Berechnung**

#### **1. Embedding-Schichten: Token Embedding & Position Embedding**

- **Schicht:** `nn.Embedding(vocab_size, emb_dim)`
- **Parameter:** `vocab_size * emb_dim`
```python
token_embedding_params = 50257 * 768 = 38,597,376
```
- **Schicht:** `nn.Embedding(context_length, emb_dim)`
- **Parameter:** `context_length * emb_dim`
```python
position_embedding_params = 1024 * 768 = 786,432
```
**Gesamtzahl der Embedding-Parameter**
```python
embedding_params = token_embedding_params + position_embedding_params
embedding_params = 38,597,376 + 786,432 = 39,383,808
```
#### **2. Transformer-Blöcke**

Es gibt 12 Transformer-Blöcke. Daher berechnen wir die Parameter für einen Block und multiplizieren anschließend mit 12.

**Parameter pro Transformer-Block**

**a. Multi-Head Attention**

- **Komponenten:**
- **Query Linear Layer (`W_query`):** `nn.Linear(emb_dim, emb_dim, bias=False)`
- **Key Linear Layer (`W_key`):** `nn.Linear(emb_dim, emb_dim, bias=False)`
- **Value Linear Layer (`W_value`):** `nn.Linear(emb_dim, emb_dim, bias=False)`
- **Output Projection (`out_proj`):** `nn.Linear(emb_dim, emb_dim)`
- **Berechnungen:**

- **Jede von `W_query`, `W_key`, `W_value`:**

```python
qkv_params = emb_dim * emb_dim = 768 * 768 = 589,824
```

Da es drei solche Layer gibt:

```python
total_qkv_params = 3 * qkv_params = 3 * 589,824 = 1,769,472
```

- **Output Projection (`out_proj`):**

```python
out_proj_params = (emb_dim * emb_dim) + emb_dim = (768 * 768) + 768 = 589,824 + 768 = 590,592
```

- **Gesamtzahl der Multi-Head-Attention-Parameter:**

```python
mha_params = total_qkv_params + out_proj_params
mha_params = 1,769,472 + 590,592 = 2,360,064
```

**b. FeedForward Network**

- **Komponenten:**
- **Erster Linear Layer:** `nn.Linear(emb_dim, 4 * emb_dim)`
- **Zweiter Linear Layer:** `nn.Linear(4 * emb_dim, emb_dim)`
- **Berechnungen:**

- **Erster Linear Layer:**

```python
ff_first_layer_params = (emb_dim * 4 * emb_dim) + (4 * emb_dim)
ff_first_layer_params = (768 * 3072) + 3072 = 2,359,296 + 3,072 = 2,362,368
```

- **Zweiter Linear Layer:**

```python
ff_second_layer_params = (4 * emb_dim * emb_dim) + emb_dim
ff_second_layer_params = (3072 * 768) + 768 = 2,359,296 + 768 = 2,360,064
```

- **Gesamtzahl der FeedForward-Parameter:**

```python
ff_params = ff_first_layer_params + ff_second_layer_params
ff_params = 2,362,368 + 2,360,064 = 4,722,432
```

**c. Layer-Normalisierungen**

- **Komponenten:**
- Zwei `LayerNorm`-Instanzen pro Block.
- Jede `LayerNorm` verfügt über `2 * emb_dim` Parameter (Skalierung und Verschiebung).
- **Berechnungen:**

```python
pythonCopy codelayer_norm_params_per_block = 2 * (2 * emb_dim) = 2 * 768 * 2 = 3,072
```

**d. Gesamtzahl der Parameter pro Transformer-Block**
```python
pythonCopy codeparams_per_block = mha_params + ff_params + layer_norm_params_per_block
params_per_block = 2,360,064 + 4,722,432 + 3,072 = 7,085,568
```
**Gesamtzahl der Parameter für alle Transformer-Blöcke**
```python
pythonCopy codetotal_transformer_blocks_params = params_per_block * n_layers
total_transformer_blocks_params = 7,085,568 * 12 = 85,026,816
```
#### **3. Letzte Schichten**

**a. Normalisierung der letzten Schicht**

- **Parameter:** `2 * emb_dim` (Skalierung und Verschiebung)
```python
pythonCopy codefinal_layer_norm_params = 2 * 768 = 1,536
```
**b. Output-Projektionsschicht (`out_head`)**

- **Schicht:** `nn.Linear(emb_dim, vocab_size, bias=False)`
- **Parameter:** `emb_dim * vocab_size`
```python
pythonCopy codeoutput_projection_params = 768 * 50257 = 38,597,376
```
#### **4. Alle Parameter zusammenzählen**
```python
pythonCopy codetotal_params = (
embedding_params +
total_transformer_blocks_params +
final_layer_norm_params +
output_projection_params
)
total_params = (
39,383,808 +
85,026,816 +
1,536 +
38,597,376
)
total_params = 163,009,536
```
## Text generieren

Bei einem Modell, das wie das vorherige den nächsten Token vorhersagt, müssen lediglich die Werte des letzten Tokens aus der Ausgabe genommen werden (da es sich dabei um die Werte des vorhergesagten Tokens handelt). Dies ist ein **Wert pro Eintrag im Vokabular**. Anschließend wird die `softmax`-Funktion verwendet, um die Dimensionen in Wahrscheinlichkeiten zu normalisieren, deren Summe 1 ergibt. Danach wird der Index des größten Eintrags ermittelt, der dem Index des Wortes innerhalb des Vokabulars entspricht.

Code from [https://github.com/rasbt/LLMs-from-scratch/blob/main/ch04/01_main-chapter-code/ch04.ipynb](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch04/01_main-chapter-code/ch04.ipynb):<sup>[[1]](#references)</sup>
```python
def generate_text_simple(model, idx, max_new_tokens, context_size):
# idx is (batch, n_tokens) array of indices in the current context
for _ in range(max_new_tokens):

# Crop current context if it exceeds the supported context size
# E.g., if LLM supports only 5 tokens, and the context size is 10
# then only the last 5 tokens are used as context
idx_cond = idx[:, -context_size:]

# Get the predictions
with torch.no_grad():
logits = model(idx_cond)

# Focus only on the last time step
# (batch, n_tokens, vocab_size) becomes (batch, vocab_size)
logits = logits[:, -1, :]

# Apply softmax to get probabilities
probas = torch.softmax(logits, dim=-1)  # (batch, vocab_size)

# Get the idx of the vocab entry with the highest probability value
idx_next = torch.argmax(probas, dim=-1, keepdim=True)  # (batch, 1)

# Append sampled index to the running sequence
idx = torch.cat((idx, idx_next), dim=1)  # (batch, n_tokens+1)

return idx


start_context = "Hello, I am"

encoded = tokenizer.encode(start_context)
print("encoded:", encoded)

encoded_tensor = torch.tensor(encoded).unsqueeze(0)
print("encoded_tensor.shape:", encoded_tensor.shape)

model.eval() # disable dropout

out = generate_text_simple(
model=model,
idx=encoded_tensor,
max_new_tokens=6,
context_size=GPT_CONFIG_124M["context_length"]
)

print("Output:", out)
print("Output length:", len(out[0]))
```
## Referenzen

- [1] [LLMs-from-scratch, Kapitel-4-Code (rasbt/LLMs-from-scratch, GitHub)](https://github.com/rasbt/LLMs-from-scratch/blob/main/ch04/01_main-chapter-code/ch04.ipynb)
- [2] [Ein Large Language Model erstellen (von Grund auf) - Manning](https://www.manning.com/books/build-a-large-language-model-from-scratch)

{{#include ../../banners/hacktricks-training.md}}
